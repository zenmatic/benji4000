def square(n)
    let n = n * n;
    return n;
end

def main()

    # do some math
    let a = 2 * (7 - 2);
    let x = square(a);

    # show it
    print("x is currently:");
    print(x);

    return x;
end